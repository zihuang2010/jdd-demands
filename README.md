# 需求资料仓

本仓用于保存融合项目需求资料，挂载在主仓 `task/demands/`。

## 目录约定

- `active/`：进行中的需求。
- `archive/`：已发布并稳定后的归档需求。

具体需求目录通常包含：

- `CONTEXT.md`
- `impact-map.md`
- `system-design.md`
- `测试用例.md`
- `发布执行手册.md`

需求实例下的 `plans/` 为本地执行计划目录，默认不进入 Git；模板目录中的 `plans/` 模板可以追踪。
