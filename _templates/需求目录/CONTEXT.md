# 需求上下文

## 需求概述

- 需求名称：`<填写需求名称>`
- 需求英文名：`<kebab-case，例如 wecom-aggregate-chat>`
- 当前阶段：需求澄清 / 设计 / 开发 / 自测 / dev 联调 / test 提测 / 发布准备 / 已发布
- 涉及应用：
  - `<应用名>`：`<仓库路径>`，功能分支 `<feature/开发者/需求英文名>`

## 范围边界

### 本期实现

- `<功能点 1>`
- `<功能点 2>`

### 本期不实现

- `<明确不做的能力或后续验收场景>`

## 当前进展

| 日期 | 阶段 | 进展 | 证据 |
| --- | --- | --- | --- |
| `<YYYY-MM-DD>` | `<阶段>` | `<完成或阻塞事项>` | `<命令/报告/日志/截图路径>` |

## 关键结论

- `<设计口径、接口口径、环境口径、用户确认结论>`

## 待办与阻塞

| 优先级 | 事项 | 负责人 | 状态 | 说明 |
| --- | --- | --- | --- | --- |
| P0 | `<事项>` | `<负责人>` | 待处理 / 阻塞 / 已完成 | `<说明>` |

## 续期恢复提示

继续本需求时，Codex 必须先读取：

1. 本文件。
2. `impact-map.md`。
3. 当前 `plans/*.md`。
4. `system-design.md`。
5. `测试用例.md`。
6. `发布执行手册.md`。

然后输出“续期恢复结果”，不得凭对话记忆继续。
