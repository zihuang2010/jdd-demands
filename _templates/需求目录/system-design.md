# 系统设计

> 编辑本文件前先阅读 `docs/standards/系统设计规范.md`，并按融合系统接口、数据库、环境和发布规范设计。

## 1. 背景与目标

- 背景：
- 目标：
- 非目标：

## 2. 现状复核

涉及已有业务、已有接口、已有表、已有状态流、已有 MQ/Job/outbox/补偿链路时，先完成既有业务复核。

| 复核项 | 当前实现 | 本次处理方式 |
| --- | --- | --- |
| 入口 | Controller/RPC/Feign/Job/MQ Consumer/页面 | 复用/扩展/迁移/废弃 |
| 数据模型 | 表、唯一键、索引、逻辑删除、状态字段 |  |
| 幂等与补偿 | 幂等键、唯一索引、重试、outbox、Job |  |
| 通知或事件 | eventType、eventReason、payload、客户端语义 |  |
| 调用链 | 上游、下游、事务边界、afterCommit、MQ/Job 时机 |  |

## 3. 总体方案

- 涉及应用：
- 关键流程：
- 设计取舍：

## 4. 接口设计

新接口遵守 `docs/standards/系统规范文档/接口规范.md`。对外客户端和前端禁止调用 `/rpc/`。

| 接口 | 方法 | 角色路径 | 鉴权边界 | 说明 |
| --- | --- | --- | --- | --- |
|  | GET/POST | `/服务名/角色/v1/业务/动作` |  |  |

### 请求示例

```json
{}
```

### 响应示例

```json
{}
```

## 5. 数据库设计

| 表 | 变更 | 说明 | SQL 路径 |
| --- | --- | --- | --- |
|  | 无/新增/修改/数据修复 |  |  |

## 6. 状态、幂等、异常与补偿

- 状态枚举：
- 幂等键：
- 重复请求或重复回调：
- 下游失败：
- 事务边界：
- 补偿或重放：
- 业务异常与错误码：

## 7. 配置、MQ 与任务

| 类型 | 名称 | 所属应用 | 环境 | 说明 |
| --- | --- | --- | --- | --- |
| Nacos/MQ/XXL-Job |  |  | local/dev/test/pre/prod |  |

## 8. 安全与权限

- 当前用户来源：
- 角色路径：
- 数据权限：
- 敏感字段：
- 日志脱敏：

## 9. 测试与验收

- 本地确定性验证：
- 本地单服务接口验证：
- mock 半链路验证：
- dev/test 真实链路验证：
- 发布前检查：

## 10. 文档回写

- `CONTEXT.md`：
- `impact-map.md`：
- `测试用例.md`：
- `发布执行手册.md`：
